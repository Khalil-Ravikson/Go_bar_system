import 'package:flutter/material.dart';

class AppColors {
  // Cores Base (Paleta Enterprise Dark)
  static const Color background = Color(0xFF0D0D0D); // Preto absoluto de fundo
  static const Color surface = Color(0xFF1C1C1E);    // Cinza escuro para cards e componentes
  static const Color surfaceElevated = Color(0xFF2C2C2E); // Cinza um pouco mais claro para hierarquia
  
  // Cores de Destaque (Neon & Laranja)
  static const Color primaryNeon = Color(0xFF39FF14); // Verde Neon (Ações positivas/pronto)
  static const Color primaryOrange = Color(0xFFFF8C00); // Laranja (Atenção/pendente)
  
  // Cores de Texto
  static const Color textPrimary = Color(0xFFFFFFFF); // Branco puro
  static const Color textSecondary = Color(0xFFB3B3B3); // Cinza para textos secundários
  
  // Cores de Erro/Alerta
  static const Color error = Color(0xFFFF453A);
}